Game.SQInclude("Databases\\$Effects\\$Top.nut");

// 1: $ëféËçUåÇ
function PlayEffect(iHandle, iFrame, iTimes) {
	local Effect = Game.DB.FDBEffect.GetEffectData(iHandle);
	switch(iFrame) {
		case -1:break;
		case 0:break;
	}
}
